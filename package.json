{
  "name": "haxball-headless-host",
  "version": "1.0.0",
  "main": "init.js",
  "scripts": {
    "start": "npx puppeteer browsers install chrome && node init.js server.js"
  },
  "dependencies": {
    "puppeteer": "^23.0.0"
  }
}
